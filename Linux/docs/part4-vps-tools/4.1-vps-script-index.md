# 4.1 VPS 常用脚本速查表

这一章整理原 `VPSscript.md` 中的脚本链接。脚本很方便，但也可能修改系统、安装软件、重装服务器。执行前请先阅读 [VPS 操作安全清单](4.2-vps-safety-checklist.md)。

风险等级说明：

- 低：主要查询信息，一般不修改系统。
- 中：会安装软件或修改配置。
- 高：可能重装系统、修改网络栈、影响 SSH 登录或造成数据丢失。

## DD 重装脚本

高风险。会重装系统，可能清空数据。执行前必须备份。

| 名称 | 用途 | 风险 | 原命令 |
| :--- | :--- | :--- | :--- |
| leitbogioro InstallNET | DD 重装 Debian | 高 | `wget --no-check-certificate -qO InstallNET.sh 'https://raw.githubusercontent.com/leitbogioro/Tools/master/Linux_reinstall/InstallNET.sh' && chmod a+x InstallNET.sh && bash InstallNET.sh -debian 12 -pwd 'password'` |
| MoeClub InstallNET | DD 重装 Debian | 高 | `bash <(wget --no-check-certificate -qO- 'https://raw.githubusercontent.com/MoeClub/Note/master/InstallNET.sh') -d 11 -v 64 -p 密码 -port 端口 -a -firmware` |
| DD Windows | 安装 Windows 系统 | 高 | `wget --no-check-certificate -qO InstallNET.sh 'https://raw.githubusercontent.com/leitbogioro/Tools/master/Linux_reinstall/InstallNET.sh' && chmod a+x InstallNET.sh && bash InstallNET.sh -windows 10 -lang "cn"` |

原笔记中 Windows 默认信息：

```text
账户：Administrator
密码：Teddysun.com
```

如果使用公开脚本安装系统，安装完成后第一时间修改默认密码。

## 综合测试脚本

| 名称 | 用途 | 风险 | 原命令 |
| :--- | :--- | :--- | :--- |
| 融合怪 | VPS 综合测试 | 低 | `bash <(wget -qO- --no-check-certificate https://gitlab.com/spiritysdx/za/-/raw/main/ecs.sh)` |
| NodeBench | VPS 综合测试 | 低 | `bash <(curl -sL https://raw.githubusercontent.com/LloydAsp/NodeBench/main/NodeBench.sh)` |
| yabs | CPU、磁盘、网络测试 | 低 | `curl -sL yabs.sh \| bash` |
| yabs Geekbench 5 | 使用 GB5 测试 | 低 | `curl -sL yabs.sh \| bash -5` |

## 性能测试

| 名称 | 用途 | 风险 | 原命令 |
| :--- | :--- | :--- | :--- |
| GB5 专测 | Geekbench 5 测试 | 低 | `bash <(curl -sL bash.icu/gb5)` |

## 流媒体与 IP 质量测试

| 名称 | 用途 | 风险 | 原命令 |
| :--- | :--- | :--- | :--- |
| media.ispvps.com | 流媒体解锁检测 | 低 | `bash <(curl -L -s media.ispvps.com)` |
| check.unlock.media | 原生检测 | 低 | `bash <(curl -L -s check.unlock.media)` |
| RegionRestrictionCheck | 流媒体区域检测 | 低 | `bash <(curl -L -s https://github.com/1-stream/RegionRestrictionCheck/raw/main/check.sh)` |
| IP.Check.Place | IP 质量体检 | 低 | `bash <(curl -sL IP.Check.Place)` |
| ChatGPT Android | ChatGPT App 检测 | 低 | `curl android.chat.openai.com` |
| ChatGPT iOS | ChatGPT App 检测 | 低 | `curl ios.chat.openai.com` |

原笔记中的结果解析链接：

```text
https://www.nodeseek.com/post-31717-1
```

## 测速脚本

| 名称 | 用途 | 风险 | 原命令 |
| :--- | :--- | :--- | :--- |
| Speedtest | 三网测速 | 低 | `bash <(curl -sL bash.icu/speedtest)` |
| Taier | 三网测速 | 低 | `bash <(curl -sL res.yserver.ink/taier.sh)` |
| hyperspeed | 多节点测速 | 低 | `bash <(curl -Lso- https://bench.im/hyperspeed)` |
| network-speed | 全球测速 | 低 | `curl -sL network-speed.xyz \| bash` |

## 回程测试

| 名称 | 用途 | 风险 | 原命令 |
| :--- | :--- | :--- | :--- |
| backtrace | 直接显示回程 | 低 | `curl https://raw.githubusercontent.com/ludashi2020/backtrace/main/install.sh -sSf \| sh` |
| testrace | 详细回程测试 | 低 | `wget https://ghproxy.com/https://raw.githubusercontent.com/vpsxb/testrace/main/testrace.sh -O testrace.sh && bash testrace.sh` |

## 功能脚本

| 名称 | 用途 | 风险 | 原命令 |
| :--- | :--- | :--- | :--- |
| Fail2ban | 防 SSH 爆破 | 中 | `wget --no-check-certificate https://raw.githubusercontent.com/FunctionClub/Fail2ban/master/fail2ban.sh && bash fail2ban.sh 2>&1 \| tee fail2ban.log` |
| 开启 BBR | 调整 TCP 拥塞控制 | 中 | 见下方命令 |
| 多功能 BBR 脚本 | 网络栈调优 | 中 | `wget -N --no-check-certificate "https://gist.github.com/zeruns/a0ec603f20d1b86de6a774a8ba27588f/raw/4f9957ae23f5efb2bb7c57a198ae2cffebfb1c56/tcp.sh" && chmod +x tcp.sh && ./tcp.sh` |
| TCP 窗口调优 | 网络参数调整 | 中 | `wget http://sh.nekoneko.cloud/tools.sh -O tools.sh && bash tools.sh` |
| 查看访问优先级 | 查看出口 IP | 低 | `curl ip.sb` |
| 添加 SWAP | 修改内存交换配置 | 中 | `wget https://www.moerats.com/usr/shell/swap.sh && bash swap.sh` |
| 25 端口测试 | 测试 SMTP 端口 | 低 | `telnet smtp.aol.com 25` |

开启 BBR 的原始命令：

```bash
echo "net.core.default_qdisc=fq" >> /etc/sysctl.conf
echo "net.ipv4.tcp_congestion_control=bbr" >> /etc/sysctl.conf
sysctl -p
sysctl net.ipv4.tcp_available_congestion_control
lsmod | grep bbr
```

## 常用环境与软件安装

| 名称 | 用途 | 风险 | 原命令 |
| :--- | :--- | :--- | :--- |
| Docker 官方脚本 | 安装 Docker | 中 | `curl -sSL https://get.docker.com/ \| sh` |
| Docker 国内脚本 | 安装 Docker | 中 | `curl -sSL https://get.daocloud.io/docker \| sh` |
| Python 安装脚本 | 安装 Python 环境 | 中 | `curl -O https://raw.githubusercontent.com/lx969788249/lxspacepy/master/pyinstall.sh && chmod +x pyinstall.sh && ./pyinstall.sh` |
| WARP | 安装 Cloudflare WARP | 中 | `wget -N https://gitlab.com/fscarmen/warp/-/raw/main/menu.sh && bash menu.sh` |
| Aria2 | 安装 Aria2 | 中 | `wget -N git.io/aria2.sh && chmod +x aria2.sh && ./aria2.sh` |
| aaPanel | 安装宝塔国际版 | 中 | `URL=https://www.aapanel.com/script/install_7.0_en.sh && if [ -f /usr/bin/curl ];then curl -ksSO "$URL" ;else wget --no-check-certificate -O install_7.0_en.sh "$URL";fi;bash install_7.0_en.sh aapanel` |
| 宝塔 | 安装宝塔面板 | 中 | `url=https://download.bt.cn/install/install_lts.sh;if [ -f /usr/bin/curl ];then curl -sSO $url;else wget -O install_lts.sh $url;fi;bash install_lts.sh ed8484bec` |

原笔记中的宝塔开心版入口：

```text
https://bt.sb/bbs/forum-37-1.html
```

这类非官方修改版面板风险较高，不建议在重要服务器上使用。

## 综合功能脚本

| 名称 | 用途 | 风险 | 原命令 |
| :--- | :--- | :--- | :--- |
| 科技 lion | 综合工具箱 | 中 | `curl -sS -O https://kejilion.pro/kejilion.sh && chmod +x kejilion.sh && ./kejilion.sh` |
| SKY-BOX | 综合工具箱 | 中 | `wget -O box.sh https://raw.githubusercontent.com/BlueSkyXN/SKY-BOX/main/box.sh && chmod +x box.sh && clear && ./box.sh` |

## 原始来源

原笔记中的论坛来源：

```text
https://www.nodeseek.com/post-143131-1
```

旧版 `VPSscript.md` 的脚本命令已经整理在本页。对应关系见 [旧内容迁移索引](../appendix/original-content-map.md)。
