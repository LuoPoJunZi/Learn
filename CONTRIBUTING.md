# 参与贡献

感谢你帮助完善 Learn。本仓库面向初学者，贡献的首要目标是让内容更容易理解、查找和验证，而不是单纯增加文件数量。

## 可以贡献什么

- 修正教程中的错误、过时命令和失效链接。
- 补充概念解释、典型场景、常见误区和安全提醒。
- 增加小而完整、能够独立运行的代码案例。
- 改善 README 导航、学习路线和旧内容迁移说明。
- 标准化现有代码，但要保留原案例的学习目的和必要接口。

## 开始之前

1. 先阅读对应专题的 `README.md` 和学习导读。
2. 检查仓库中是否已有相同主题，避免重复创建近似教程。
3. 修改旧内容前确认它被哪些 README、脚本或案例引用。
4. 涉及外部资料时优先参考官方文档、论文或原项目，并标明来源。

仓库中的 `AGENTS.md` 是本地协作说明，不属于公开项目文件，不要提交。

## 目录与命名

- 每个一级主题目录必须有 `README.md`。
- 新目录和普通文档优先使用小写短横线命名，例如 `data-processing`。
- 语言或工具约定的入口名可以保留，例如 `README.md`、`main.py`、`main.m` 和 `index.html`。
- 已有的空格路径和历史算法文件属于兼容内容，不要只为统一外观批量改名。
- 文件移动或改名后，必须同步更新所有本地链接和迁移说明。

## 文档要求

面向新手的主要教程应尽量包含：

- 适合人群和学习目标。
- 命令或代码执行前的概念解释。
- 可以验证结果的完整步骤。
- 常见错误、安全边界和下一步入口。
- 外部资料的用途说明与来源链接。

不要留下没有入口的孤立文档，也不要用删除“后续建议”的方式掩盖尚未落实的内容。

## 代码要求

- 遵守 [示例代码规范](docs/CODE_STYLE.md) 和仓库根目录的 `.editorconfig`。
- 新 Python 示例优先使用明确类型、`main()` 入口和可重复输入。
- 网络请求设置超时，凭据从环境变量或安全配置读取。
- 删除、移动、覆盖和批量发送操作默认提供预演或明确确认。
- Matlab 历史案例可以保留原接口；修改时要同步检查主函数名、入口脚本和 README。

## 提交前检查

在仓库根目录运行统一检查：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\check-all.ps1
```

检查内容包括仓库结构、跨平台路径、误提交缓存、Python/JavaScript/PowerShell/HTML/Matlab 代码、Markdown 链接、代码块和 Git 空白错误。

修改可运行案例时，还要在安全条件下实际运行对应入口。涉及真实账号、网络写入、文件删除或远程服务器的脚本，不要拿真实数据直接测试。

## 提交与 Pull Request

提交信息使用简短、明确的动词，例如：

```text
docs: clarify python virtual environments
feat: add a safe file-sorting example
fix: correct matlab function name
chore: standardize repository checks
```

Pull Request 中请说明修改目的、影响目录、验证方式，以及是否移动了旧内容。结构调整需要明确写出旧文件的新位置，确保原内容仍然可查找。

## 安全与版权

- 不要提交密码、Token、SSH 私钥、Cookie、真实服务器地址或个人数据。
- 不直接复制许可证不明确的大型教程、模板或代码仓库。
- 引用外部内容时遵守其许可证和署名要求；本仓库中的改写也要保留必要来源。
- 发现疑似凭据泄露时，不要在公开 Issue 中粘贴密钥原文。
